# ログインページ新規登録機能 TDD プラン

## 背景
- 現在のログインページはメール/パスワード、Google、ゲストログインのみを提供しており、新規登録導線がない。
- Supabase Auth を用いたメールアドレス登録を同ページ内で完結させ、認証導線を増やす。

## 目的
- ログインページに「新規登録」フローを追加し、自己登録を可能にする。
- 既存のログイン・Google・ゲストログインの動作を損なわずに実装する。
- TDD で安全に追加するため、先にテスト観点を明確化する。

## スコープ
- フロントエンド（Next.js / React 19）ログインページの UI・状態管理・Supabase 認証呼び出し。
- テスト（Vitest + Testing Library）と Supabase ブラウザクライアントのテスト用モック拡張。

## アウトオブスコープ
- バックエンド（FastAPI）や Supabase ポリシー設定の変更。
- OAuth プロバイダの追加/設定変更。
- パスワードリセットやメールアドレス確認メール本文の変更。

## 前提・制約
- Supabase のメール認証が有効である前提（`signUp` 成功時、確認メール送信が必要なケースを想定）。
- 型安全（TypeScript strict）、`any` 禁止。UI は shadcn/ui + Tailwind v4 に合わせる。
- 既存の URL クエリによるエラー表示（`error` パラメータの toast）は保持する。

## 機能要件
1. ログイン/新規登録を同一ページでタブまたはトグルで切替できる。初期表示はログイン。
2. 新規登録フォーム入力項目: メールアドレス、パスワード、パスワード確認。
3. バリデーション:
   - メール: HTML `type="email"` による基本検証。
   - パスワード: 8 文字以上必須。
   - 確認用パスワードが一致しない場合は送信を拒否しエラー表示。
4. 送信時の共通挙動:
   - 送信中は入力/ボタンを非活性化し、多重送信を防ぐ。
   - Supabase ブラウザクライアントを用いて認証 API を呼び出す。
5. 新規登録の Supabase 呼び出し:
   - `supabase.auth.signUp({ email, password, options: { emailRedirectTo: <origin>/auth/callback } })` を実行する。
6. 新規登録成功時:
   - まず toast で「確認メールを送信しました。メール内のリンクを開いて認証を完了してください。」を表示。
   - Supabase からセッションが返るケースでは `/` に遷移し `router.refresh()` を呼ぶ。
   - セッションが無い場合はページ滞在のままフォームをリセットする。
7. 新規登録失敗時:
   - Supabase が返すメッセージを toast に表示し、ページ遷移しない。
8. ログイン既存動作の維持:
   - メール/パスワード、Google、ゲストログインの処理とトースト内容を変更しない。
9. UI/UX:
   - ログインと新規登録で文言とボタンラベルを切り替える（例: 「ログイン」「登録」）。
   - タブ切替時にフォーム入力値とエラー状態を初期化する。

## TDD で実装するテスト観点（Vitest + @testing-library/react）
- デフォルトでログインタブが選択され、送信ボタン文言が「ログイン」である。
- 新規登録タブを選ぶと登録用フィールド（パスワード確認）が表示され、送信ボタン文言が「登録」に変わる。
- パスワードが 8 文字未満または確認不一致の場合、`signUp` は呼ばれずバリデーションエラーが表示される。
- `signUp` 成功（セッション無し）の場合、成功 toast が呼ばれ、フォームがリセットされるがリダイレクトされない。
- `signUp` 成功（セッションあり）の場合、成功 toast の後に `/` への遷移と `router.refresh` が呼ばれる。
- `signUp` エラー時にエラートーストが表示され、遷移しない。
- 送信中は入力とボタンが disabled になり、ローディング文言が表示される。
- 既存ログインフローのリグレッション: ログインタブで送信すると `signInWithPassword` が呼ばれ、成功時に `/` へ遷移する。

## 実装タスク（推奨順）
1. **テスト用 Supabase クライアントモック拡張**: `frontend/lib/supabase/client.ts` のテスト用クライアントに `signInWithPassword`, `signUp`, `signInAnonymously`, `signInWithOAuth` を追加し、テストから挙動を差し替えられるようにする。
2. **UI/ロジック拡張**: ログインページにモード切替（ログイン/新規登録）を追加し、登録フォームのバリデーション・サインアップ処理・トースト/リダイレクト分岐を実装。ローディングとフォームリセットも含める。
3. **テスト追加**: `frontend/__tests__/app/login-page.test.tsx` を新規作成し、上記テスト観点をカバー。`useRouter` と `createClient`、`sonner` をモックして TDD を進める。
4. **リグレッション確認**: `npm run test --prefix frontend`（時間短縮のため対象テストのみピンポイント実行も可）で既存テストが通ることを確認。

## 完了条件
- 上記テスト観点を満たす Vitest が追加され、ローカルでグリーンになること。
- ログインページで新規登録フローを UI 上で利用可能になり、既存ログイン機能に回帰がないこと。
