# 認証について

## 現状

現在、このアプリケーションには**フロントエンドのログイン機能が実装されていません**。

- バックエンドはすべての API リクエストで JWT 認証を要求
- フロントエンドは localStorage から既存トークンを探すのみ
- ログイン画面や Supabase 認証クライアントが未実装

## 開発環境での回避策

`backend/.env`に以下を追加することで、開発中は認証をスキップできます：

```bash
DISABLE_AUTH=true
```

## 今後の実装予定

本番環境で使用するには、以下の実装が必要です：

1. Supabase 認証クライアントの統合
2. ログイン/ログアウト画面の実装
3. JWT トークンの自動管理
